# run_sota.py
# Patent-grade wardrobe analyzer runner
from WARDROBE_ANALYZER_SOTA import analyze_wardrobe

IMAGE_PATH = r"C:\Users\nadai\OneDrive\Pictures\Screenshot 2025-09-26 172736.png"  # Change this path

if __name__ == "__main__":
    print("="*60)
    print("PATENT-GRADE WARDROBE COLOR ANALYZER")
    print("="*60)
    print(f"Analyzing image: {IMAGE_PATH}")
    
    res = analyze_wardrobe(image_path=IMAGE_PATH, visualize=True)
    
    print("\n" + "="*60)
    print("DETAILED ANALYSIS RESULTS")
    print("="*60)
    
    print(f"Total Regions Analyzed: {res['detection_summary']['total_regions']}")
    print(f"Detectron2 Instances: {res['detection_summary']['detectron2_regions']}")
    print(f"Grid Regions: {res['detection_summary']['grid_regions']}")
    print(f"Distinct Colors Identified: {len(res['current_colors'])}")
    
    print(f"\nColor List: {', '.join(res['current_colors'])}")
    
    print("\nDetailed Color Distribution:")
    for i, (color, freq) in enumerate(res["color_distribution"].items(), 1):
        print(f"  {i:2d}. {color:>8s}: {freq:6.1%}")
    
    print("\n" + "="*60)
    print("ANALYSIS COMPLETE - PATENT-READY RESULTS")
    print("="*60)
